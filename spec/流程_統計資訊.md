# 統計資訊流程（事件頻率 + 事件統計表）

## 觸發

主畫面左側垂直主選單點「統計資訊」，切換至統計資訊頁。頁面含兩個展示區：

1. **事件頻率圖**（ECharts 長條圖）：呈現後端各 `event`（API、kpFunExt、連線驗證等）之時段發生頻率。**各事件各成一系列、固定顏色**，使用者可點圖表之圖例（legend）切換個別事件之顯示/隱藏以聚焦——供日後**趨勢辨認**與**危險識別**（如鎖定某錯誤事件觀察其時段爆量）。另可切時間範圍（1時 / 4時 / 8時 / 1日，前端重採樣）。
2. **事件統計表**：各 `event` 為一列，依「最近1日」發生數由多到少排序，欄位為「最近1日 / 8時 / 4時 / 1時」之計數。

資料來源皆為後端 `getStaEvent`（讀 `logFd` 以 `staEvent` 依時段 + event 聚合）；統計表之各時間窗計數由前端就小時 bucket 加總最後 N 桶得出。

## 重要流程

- **E2E-001**
  - title：事件頻率圖各事件分系列
  - description：進站預設頁即統計資訊頁（左側選單首項、預設選中），使用者以 `?token=sys` 進站後直接看到統計頁。資料中**所有事件各成一系列、各自顏色**（不把所有事件視為一種，可區分趨勢）；個別事件之顯示/隱藏由 echarts 圖例（legend）提供，不另建 checkbox。以合成 log（確定性資料）餵圖。圖表區 x 軸為相對今日之日期、且 canvas GPU 渲染非決定性，故 pixel baseline 以紅框標註並「貼圖覆蓋」（per-item ref 凍結真實圖表、非黑塊）；語意（選單/標題/控制/統計表逐事件成列/canvas）讀 live DOM，數值正確性由 `server/staLogs/test_staEvent.mjs` 保證。
  - flow：
    - 測試資料：合成 log 一組（多種 event：`verifyConn`、`kpfun-getApisList`、`kpfun-saveApi`、`kpfun-getWebInfor`、`api/getUserByToken`、`verifyClientUser`、`api/syncAndReplaceTabs`（已知最長事件名，供版面邊界驗證）數筆，落近 N 天），寫入測試專用 log 資料夾並令後端以該資料夾為 `logFd`；base seed API 樹。
    - 操作：開啟 `<baseUrl>/?token=sys`（進站預設頁即統計頁，自動載入）。
    - 驗證：
      1. 語意：左選單「統計資訊」active；標題該語系「Statistics Information」/「統計資訊」；控制列含「Time range」/「時間範圍」；統計表逐事件成列且 ≥ 2 種（圖表系列與表列同源於全事件集，故表列為「各事件各成一系列」之可觀察證據），含已知最長事件名 `api/syncAndReplaceTabs`；圖表 canvas 存在。
      1-2. 版面規格：控制列 label 與其控制項以結構性對齊（不依賴寫死寬度），中英文語系均須對齊；統計表事件名欄於極長名稱時須換行，不得撐破表格或使頁面水平溢出。
      2. 視覺：baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-001-chart-all.png`（pixelmatch 反鋸齒感知 + maxDiffPixels 容差，非 byte-exact）；圖表區以紅框標註，並以 per-item ref（`_staref-{lang}-E2E-001-chart-all.png`）貼圖覆蓋凍結。
    - 雙語：eng / cht 各一輪（各自 fresh browser）。
    - 清理：移除測試 log 資料夾合成檔；本檔全跑完以預設 settings 重啟後端還原 `logFd`；base seed 保留。

- **E2E-003**
  - title：事件統計表依最近1日排序呈現各時間窗計數
  - description：統計頁下方事件統計表，各 `event` 為一列，依「最近1日」計數由多到少排序，欄位含最近1日 / 8時 / 4時 / 1時。驗證表頭該語系、至少一列、排序正確（上而下多→少）。表格計數會隨後端執行期 log 累積而漂移，pixel baseline 以貼圖覆蓋凍結；排序與內容正確性讀 live DOM 斷言。
  - flow：
    - 測試資料：同 E2E-001。
    - 操作：進統計頁 → 捲動至事件統計表。
    - 驗證：
      1. 語意：表頭含該語系「Event」/「事件」與「Last 1 day」/「最近1日」；至少一列；逐列驗「最近1日」計數遞減（上而下多→少）。
      2. 視覺：baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-003-table.png`（pixelmatch 反鋸齒感知 + maxDiffPixels 容差，非 byte-exact）；統計表區以紅框標註，並以 per-item ref（`_staref-{lang}-E2E-003-table.png`）貼圖覆蓋凍結。
    - 雙語：eng / cht 各一輪。
    - 清理：同 E2E-001。

- **E2E-004**
  - title：切換時間範圍下拉，事件頻率圖依重採樣重繪
  - description：承統計頁（時間範圍下拉預設「1小時」）。使用者將時間範圍下拉切為「1天」，前端依 timeGroup 對頻率資料重採樣（同日之時段合併加總），事件頻率圖之 x 軸與長條隨之重繪。預期時間範圍下拉顯示所選「1天」、圖表 canvas 重繪後仍存在。粒度：圖表繪於 canvas 為非決定性（x 軸日期相對今日漂移 + GPU 光柵化），故與 E2E-001 同以貼圖覆蓋凍結；重採樣正確性以「時間範圍（下拉）→ resampledData → chartOption」之綁定 + `server/staLogs/test_staEvent.mjs` 數值保證，canvas 內部不做 DOM/pixel introspection（設計取捨見補充表格）。
  - flow：
    - 測試資料：同 E2E-001（合成 log）。
    - 操作：進統計頁 →（將「時間範圍」/「Time range」下拉切為「1 day」/「1天」）。
    - 驗證：
      1. 語意：時間範圍下拉值為「1天」（顯示該語系「1 day」/「1天」）；圖表 canvas 重繪後仍存在。**重採樣正確性以「下拉選取狀態 → resampledData」之直接綁定保證**——canvas 內部長條/座標因繪於 canvas 不做 introspection（屬貼圖凍結之設計取捨），數值另由 `test_staEvent` 守。
      2. 視覺：baseline `test/pics/stainfor/stainfor-{eng,cht}-E2E-004-timegroup-1day.png`（pixelmatch 反鋸齒感知 + maxDiffPixels 容差，非 byte-exact）；圖表區以紅框標註，並以 per-item ref 貼圖覆蓋凍結。
    - 雙語：eng / cht 各一輪。
    - 清理：同 E2E-001。

## 執行流程

```
001  使用者點左側垂直主選單「統計資訊」按鈕（@click 綁 onClickMainMenu）  [src/components/LayoutContent.vue:12-21]
        onClickMainMenu 將 section 設為該選單 id（'stats'）  [src/components/LayoutContent.vue:558-561]
        section === 'stats' 成立 → 掛載 <LayoutContentStats>（傳入 panelHeight）  [src/components/LayoutContent.vue:267-270]
002  LayoutContentStats mounted 呼叫 load()  [src/components/LayoutContentStats.vue:153-156]
        load() 設 loading=true、清空 errMsg，呼叫後端 getStaEvent(7, 'hr')，reject 時 throw 至 .catch  [src/components/LayoutContentStats.vue:342-346]
        template 立即渲染標題、控制區、圖表區與統計表區骨架；loading 時圖表區以與圖同高之轉圈佔位、統計表以轉圈佔位（先到先畫，ADR-028）  [src/components/LayoutContentStats.vue:38-68]
            getStaEvent（經 wrapErrLog 包裝）呼叫 staEvent(7, 'hr', { fdLog: logFd, srLog })  [server/WWebApi.mjs:913-915]
                staEvent 為薄殼，轉呼叫 staLogsCore（worker 版）  [server/staLogs/staEvent.mjs:13-16]
                genPlan：取 fdLog（無則 './logs'）、依 timeInterval（非 'day' 一律視同 'hr'）決定 fmt 與 unit、建 kpTime（無資料時段補 { count: 0 }）  [server/staLogs/staLogsCore.mjs:35-66]
                single-flight：同參數併發呼叫共用同一 in-flight promise  [server/staLogs/staLogsCore.mjs:394-412]
                listFiles：fsTreeFolder 列 fdLog 下項目、剔除資料夾、filterVpfsByWindow 剔除窗外檔、stat 取 size/mtime  [server/staLogs/staLogsCore.mjs:69-97]
                分流：檔級快取命中（size/mtime 未變、掃描時無行被窗過濾（檔內最早行 > 該次掃描之 tStart）、且檔內最早行 > 本次 tStart）者沿用彙總，否則列入重掃；自清單消失之檔淘汰快取  [server/staLogs/staLogsCore.mjs:317-333]
                重掃清單交單一 worker；worker 之 message / error / exit 任一先到者定案並回收 worker，未回訊息即退出亦 reject（不留死 promise）  [server/staLogs/staLogsCore.callWorker.mjs:18-56]
                    worker 逐檔以 stream 逐行 parse JSON，先比 event 字串再取時間（epoch ms 數值比較），行落在 tStart 之後者就地累加至該時段桶（依 event 名計數 + 總數 count）；單檔錯誤略過不整體失敗  [server/staLogs/staLogsCore.mjs:155-228]
                合併各檔彙總、補零時段、依時間遞增排序輸出  [server/staLogs/staLogsCore.mjs:231-268, 370-390]
        .then：回傳陣列存入 freq（非陣列則空陣列）  [src/components/LayoutContentStats.vue:347-349]
        .catch：設 errMsg 為 getDataError  [src/components/LayoutContentStats.vue:350-353]
        .finally：結束等待態（loading=false，轉圈換為圖/表）  [src/components/LayoutContentStats.vue:354-356]
        computed allEvents：彙整 freq 各時段出現過之 event 名（排除 count）並排序  [src/components/LayoutContentStats.vue:168-182]
        computed resampledData：依 timeGroup 重採樣（1hr 原樣；4hr/8hr/1day 以 groupBy 同日合併加總）  [src/components/LayoutContentStats.vue:181-215]
        computed seriesKeys：資料中所有事件（= allEvents；個別事件顯示/隱藏改由 echarts legend 切換，不另維護選取狀態）  [src/components/LayoutContentStats.vue:218-223]
        computed chartOption：對 resampledData 各 seriesKey 產一長條系列、各自固定顏色（colorOf 依 allEvents 索引取色）  [src/components/LayoutContentStats.vue:237-272]
        computed eventRows：各事件就小時 bucket 加總最後 24/8/4/1 桶，依最近1日總數多→少排序（8時/4時/1時 tie-break）  [src/components/LayoutContentStats.vue:275-299]
003  使用者切時間範圍下拉（timeGroup v-model 更新）→ resampledData 重採樣 → chartOption x 軸與長條隨之更新  [src/components/LayoutContentStats.vue:16-21]
        resampledData 依新 timeGroup 重算分組  [src/components/LayoutContentStats.vue:186-214]
004  使用者點圖表圖例之某事件 → echarts legend 內建切換該系列顯示/隱藏（不改 chartOption，本專案不另實作）  [src/components/LayoutContentStats.vue:263]
```

## 補充表格

### 為何貼圖覆蓋圖/表區（非決定性來源）

| 來源 | 說明 | 處置 |
|---|---|---|
| x 軸日期標籤 | `staEvent` 時段為「相對今日往回 N 天」，每日漂移 | 貼圖覆蓋（per-item ref 凍結），不對該區做 live pixel 比對 |
| ECharts canvas | 圖例/座標軸/長條由 echarts 繪於 canvas，跨版本/時序次像素差 | 一併由 ref 覆蓋凍結 |
| 統計表計數 | 測試自身連線新增 log 至 logFd，累積使計數漂移 | 貼圖覆蓋；排序/內容正確性改讀 live DOM 斷言 |

### 語意斷言對應（雙語）

| 觀察點 | eng | cht |
|---|---|---|
| 左選單項 | Statistics | 統計資訊 |
| 頁標題 | Statistics Information | 統計資訊 |
| 選擇事件 | Select events | 選擇事件 |
| 時間範圍 | Time range | 時間範圍 |
| 表頭-事件 | Event | 事件 |
| 表頭-最近1日 | Last 1 day | 最近1日 |
